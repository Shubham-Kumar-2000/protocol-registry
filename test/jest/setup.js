const constants = require('../config/constants');

console.log(
    'Jest Setup: Environment Constants ',
    JSON.stringify(constants, null, 4),
    '\n----------------------------------------------------'
);
