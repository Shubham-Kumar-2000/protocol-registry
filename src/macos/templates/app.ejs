set this_URL to ( the clipboard as text )
<%if (terminal > 0) { %>
tell application "Terminal"
    do script "<%= command %>  '" & this_URL & "'"
    activate
end tell
<% } else { %>
do shell script "<%= command %> '" & this_URL & "'"
<% } %>
set the clipboard to ""